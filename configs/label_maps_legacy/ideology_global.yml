# Mapping des labels bruts (domain, crawl, ideology...) vers labels consolidés
# Les clés sont normalisées (lowercase + séparateurs convertis en `_`), mais
# tu peux les écrire "naturellement" : core_prepare les normalise avant lookup.

mapping:
  actionfrancaise.net: far_right
  bernardlugan.blogspot.com: far_right
  politiquemagazine.fr: right
  crawl-actionfrancaise-20251003_000000: far_right

unknown_labels:
  policy: "drop"  # drop (ignore) | keep (label_raw conservé) | other (basculer vers other_label)
  other_label: "other"
